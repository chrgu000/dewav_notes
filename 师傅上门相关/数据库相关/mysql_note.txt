验证Mysql安装
mysqladmin --version

启动 mysql：
service mysqld start

初始密码
[root@localhost ~]# grep "password" /var/log/mysqld.log

mysql> SET PASSWORD FOR 'root'@'localhost' = PASSWORD('newpass');

修改密码策略
在/etc/my.cnf文件添加
validate_password = off

修改密码
[root@localhost ~]#mysqladmin -u root password oldpass "newpass"

通过以下命令来连接到Mysql服务器
[root@host]# mysql -u root -p
Enter password:*******

启动及关闭 MySQL 服务器
ps -ef | grep mysqld

./mysqladmin -u root -p shutdown
Enter password: ******

MySQL 选择数据库
mysql> use RUNOOB;

MySQL 创建数据库
[root@host]# mysqladmin -u root -p create RUNOOB
Enter password:******
MySQL 删除数据库
[root@host]# mysqladmin -u root -p drop RUNOOB
Enter password:******

显示数据库
mysql> show databases；

查看端口
mysql> show global variables like 'port'; 


sqlyog相关
数据库表字段同步---高级工具--架构同步工具
工具---备份数据库---执行sql脚本(修改里面的数据库名称)

